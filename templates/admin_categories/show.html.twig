{% extends 'base.html.twig' %}

{% block title %}Категория{% endblock %}

{% block body %}
    <h1>Категория</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Название</th>
                <td>{{ category.name }}</td>
            </tr>
            <tr>
                <th>Изображение</th>
                <td>{{ category.image }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_admin_categories_index') }}">к списку</a>

    <a class="btn btn-success" href="{{ path('app_admin_categories_edit', {'id': category.id}) }}">редактировать</a>

    {{ include('admin_categories/_delete_form.html.twig') }}
{% endblock %}
