<form method="post" action="{{ path('app_admin_products_delete', {'id': product.id}) }}" onsubmit="return confirm('Вы действительно хотите удалить данный товар?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.id) }}">
    <button class="btn btn-danger">Удалить</button>
</form>
